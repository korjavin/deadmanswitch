{
  "name": "deadmanswitch-frontend-tests",
  "version": "1.0.0",
  "description": "Frontend tests for Heartbeat (deadmanswitch)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1"
  }
}
